package com.springunivers.repository;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;

import com.springunivers.model.Contato;

public interface ContatoRepository {// extends JpaRepository<Contato, Integer> {
	/*
	 * List<Contato> findByNome(String nome); List<Contato> findBySobrenome(String
	 * sobrenome); List<Contato> findByNomeContaining(String nome); List<Contato>
	 * findBySobrenomeContaining(String sobrenome);
	 */
}
